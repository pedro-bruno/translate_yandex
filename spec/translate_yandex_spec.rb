require "spec_helper"

describe TranslateYandex do
  it "has a version number" do
    expect(TranslateYandex::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
